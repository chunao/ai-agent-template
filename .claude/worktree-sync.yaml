# Worktree Sync Configuration
# 新しいworktreeを作成する際に、メインリポジトリからコピーするファイルを定義します。

# 同期対象ファイル
# これらのファイルはメインリポジトリから新しいworktreeにコピーされます。
# ファイルが存在しない場合はスキップされます。
sync_files:
  - .env
  - .env.local
  - .claude/settings.local.json
  - knowledge/
